# Mapa da interface iOS e Android

Este documento descreve a arquitetura da tela compartilhada `DebugSwiftAndroidPanel` e das features que ela expõe. Ele separa a tecnologia que desenha cada tela da implementação nativa que fornece os dados e executa as ações.

## Como ler as camadas

| Camada | iOS | Android |
| --- | --- | --- |
| SwiftUI compartilhado | A tela é executada como SwiftUI. | O Skip transpila o SwiftUI para Kotlin/Compose. A tela não precisa de uma segunda implementação visual em Compose. |
| UIKit legado | Controladores UIKit existentes continuam sendo usados e são hospedados pela tela SwiftUI com `UIViewControllerRepresentable`. | UIKit não existe no Android; a feature usa a tela compartilhada ou uma adaptação nativa Android. |
| Compose escrito à mão | Não se aplica. | Há Compose escrito à mão em `DebugSwiftComposeProbe.kt`, que envolve uma árvore Compose do app para contar recomposições. Ele não implementa as telas do debugger. |
| Serviços nativos | Frameworks e APIs do iOS acessados pelo `DebugSwift`. | APIs Android e integrações em Kotlin, acessadas pelas telas compartilhadas através de `DebugSwiftNativeBridge`. |

O modo do módulo está configurado como `transpiled` em [`skip.yml`](../DebugSwiftAndroid/Sources/DebugSwiftAndroid/Skip/skip.yml). O código Kotlin gerado pelo Skip fica na saída de build; os arquivos gerados não são a fonte editável da UI.

## Mapa por área e feature

| Área e features | UI no iOS | UI no Android | Implementação nativa / observações |
| --- | --- | --- | --- |
| Painel, categorias e listas | SwiftUI compartilhado | SwiftUI convertido pelo Skip para Compose | Definidos em [`DebugSwiftAndroidPanel.swift`](../DebugSwiftAndroid/Sources/DebugSwiftAndroid/DebugSwiftAndroidPanel.swift). O app de exemplo Android inicia diretamente esse painel. |
| **Network:** `http`, `websocket`, `network_injection`, `network_thresholds`, `graphql`, `network_encryption`, `har_export`, `webview_network`, `network_history` | SwiftUI compartilhado | O mesmo SwiftUI convertido para Compose | As telas de inspeção e configuração estão em [`DebugSwiftNetworkInspectorView.swift`](../DebugSwiftAndroid/Sources/DebugSwiftAndroid/DebugSwiftNetworkInspectorView.swift), [`DebugSwiftWebSocketInspectorView.swift`](../DebugSwiftAndroid/Sources/DebugSwiftAndroid/DebugSwiftWebSocketInspectorView.swift) e [`DebugSwiftNetworkInjectionView.swift`](../DebugSwiftAndroid/Sources/DebugSwiftAndroid/DebugSwiftNetworkInjectionView.swift). No Android, o bridge chama Kotlin; interceptação de HTTP/WebSocket e WebView fica em [`DebugSwiftOkHttpInterceptor.kt`](../DebugSwiftAndroid/Sources/DebugSwiftAndroid/Skip/DebugSwiftOkHttpInterceptor.kt), [`DebugSwiftOkHttpWebSocket.kt`](../DebugSwiftAndroid/Sources/DebugSwiftAndroid/Skip/DebugSwiftOkHttpWebSocket.kt) e [`DebugSwiftWebViewClient.kt`](../DebugSwiftAndroid/Sources/DebugSwiftAndroid/Skip/DebugSwiftWebViewClient.kt). A disponibilidade de histórico e WebSocket pode depender da configuração do app/plataforma. |
| **Performance:** `performance_overview`, `performance_widget`, `battery`, `disk`, `memory_warning`, `frame_drops`, `hangs`, `backtraces`, `leaks`, `thread_checker`, `super_calls` | UIKit existente, hospedado por `UIViewControllerRepresentable` | SwiftUI compartilhado convertido para Compose, usando a tela genérica de detalhes | Android obtém dados e executa ações por [`DebugSwiftNativeBridge.kt`](../DebugSwiftAndroid/Sources/DebugSwiftAndroid/Skip/DebugSwiftNativeBridge.kt) e [`AndroidDebugTools.kt`](../DebugSwiftAndroid/Sources/DebugSwiftAndroid/Skip/AndroidDebugTools.kt). A apresentação Android ainda é genérica e não replica os controladores UIKit feature por feature. |
| **Interface compartilhada:** `grid`, `touches`, `dark_mode` e controles de bordas (`colorize` no iOS, `view_borders` no Android) | SwiftUI compartilhado | SwiftUI convertido para Compose | A tela de grid e os controles chamam `DebugSwiftAndroidRuntime`; a aplicação do overlay e configurações do host usa implementação nativa. |
| **Interface adicional do iOS:** `animations`, `measurement` | SwiftUI compartilhado | — | No painel iOS, esses itens usam a tela compartilhada de configuração. |
| **Interface adicional do iOS:** `swiftui_render`, `doc_recorder`, `color_palette` | Controladores UIKit existentes, hospedados por `UIViewControllerRepresentable` | — | Mapeados por `debugInterfaceViewController(for:)` em [`DebugSwiftInterfaceSettings.swift`](../DebugSwift/Sources/Features/Interface/DebugSwiftInterfaceSettings.swift). |
| **Interface adicional do Android:** `view_hierarchy`, `animation_control`, `compose_renders`, `doc_recorder`, `measurement`, `color_palette` | — | Detalhe SwiftUI genérico convertido para Compose | A lógica específica de Android fica em Kotlin. `compose_renders` usa o probe Compose escrito à mão em [`DebugSwiftComposeProbe.kt`](../DebugSwiftAndroid/Sources/DebugSwiftAndroid/Skip/DebugSwiftComposeProbe.kt); o probe mede recomposições e não desenha a tela de configuração. |
| **Resources no iOS:** `files`, `user_defaults`, `keychain`, `persistent_data`, `core_data`, `swift_data`, `http_cookies`, `database` | Controladores UIKit existentes, hospedados por `UIViewControllerRepresentable` | — | Os controladores são selecionados por `debugResourceViewController(for:)` em [`DebugSwiftResourceViewControllers.swift`](../DebugSwift/Sources/Features/Resources/DebugSwiftResourceViewControllers.swift). SwiftData requer iOS 17 para a feature, embora o mínimo do pacote seja iOS 16. |
| **Resources compartilhado:** `security_audit` | SwiftUI compartilhado | SwiftUI convertido para Compose | A tela é `DebugSwiftSecurityAuditView`; a coleta usa o framework iOS ou a implementação Kotlin Android. |
| **Resources no Android:** `files`, `preferences`, `keychain`, `persistent_data`, `sqlite`, `realm`, `core_data` (Room), `swift_data` (object store), `cookies` | — | Detalhe SwiftUI genérico convertido para Compose | Os equivalentes Android acessam sandbox/arquivos, SharedPreferences, Android Keystore, SQLite, Realm, Room e cookies de WebView. O browser SQLite e o registro Realm ficam em [`DebugSwiftSQLiteBrowser.kt`](../DebugSwiftAndroid/Sources/DebugSwiftAndroid/Skip/DebugSwiftSQLiteBrowser.kt). Os nomes `core_data` e `swift_data` identificam equivalentes Android; não significam que Core Data ou SwiftData estejam disponíveis no Android. |
| **App compartilhado:** `console` | SwiftUI compartilhado | SwiftUI convertido para Compose | A tela compartilhada consulta e altera o logger nativo da plataforma. |
| **App no iOS:** `crashes`, `oslog_console`, `location`, `loaded_libraries`, `push_simulator`, `deep_links`, `event_bus`, `agent_debug_log` | Controladores UIKit existentes, hospedados por `UIViewControllerRepresentable` | — | Mapeados por `debugAppViewController(for:)` em [`DebugSwift.swift`](../DebugSwift/Sources/Settings/DebugSwift.swift). |
| **App com detalhe genérico:** `device_info`, `push_token`, `custom_actions`, `custom_info` | SwiftUI compartilhado | SwiftUI convertido para Compose | Os dados e ações vêm do framework iOS ou da implementação Android. O token de push é próprio de cada plataforma (APNs no iOS, FCM quando integrado no Android). |
| **App no Android:** `crashes`, `oslog_console`, `location`, `loaded_libraries`, `push_simulator`, `deep_links`, `event_bus`, `agent_debug_log` | — | Detalhe SwiftUI genérico convertido para Compose | A coleta e as ações Android ficam principalmente em [`AndroidDebugTools.kt`](../DebugSwiftAndroid/Sources/DebugSwiftAndroid/Skip/AndroidDebugTools.kt), com Logcat, intents, bibliotecas instaladas, eventos e APIs do sistema. Recursos como push dependem da integração necessária no app hospedeiro. |

## Pontos importantes para avaliar paridade

- `UIViewControllerRepresentable` é a ponte iOS para conservar telas UIKit pontuais dentro da navegação SwiftUI compartilhada. Ela não participa do build Android.
- No Android, Compose é principalmente o resultado da tradução Skip das views SwiftUI. A implementação Kotlin fornece acesso às APIs Android e a recursos que não têm equivalente SwiftUI.
- As telas `DebugSwiftFeatureDetail` são uma apresentação comum de snapshot e ações. Elas dão acesso às operações implementadas para cada feature, mas não oferecem a mesma navegação, controles e organização das telas UIKit de performance, recursos e app. Para essas linhas, a equivalência visual/funcional 1:1 ainda requer evolução específica.
- A lista e a disponibilidade de features variam por plataforma. O iOS filtra recursos conforme as APIs e configurações disponíveis; o Android inclui alguns recursos próprios, como inspeção da hierarquia View e rastreamento de recomposição Compose. Uma linha nesta tabela identifica a rota implementada, mas não afirma que as APIs das duas plataformas sejam idênticas.
- O alvo iOS do pacote está em iOS 16 no [`Package.swift`](../Package.swift). Algumas features mantêm requisitos maiores, como SwiftData no iOS 17.

## Arquivos de referência

- [`DebugSwiftAndroidPanel.swift`](../DebugSwiftAndroid/Sources/DebugSwiftAndroid/DebugSwiftAndroidPanel.swift): lista de áreas/features, seleção entre views compartilhadas, detalhes genéricos e hosts UIKit.
- [`DebugSwiftNativeBridge.kt`](../DebugSwiftAndroid/Sources/DebugSwiftAndroid/Skip/DebugSwiftNativeBridge.kt): interface entre o Swift transpilado e os serviços Kotlin.
- [`AndroidDebugTools.kt`](../DebugSwiftAndroid/Sources/DebugSwiftAndroid/Skip/AndroidDebugTools.kt): serviços, snapshots e ações nativas Android.
- [`DebugSwift.swift`](../DebugSwift/Sources/Settings/DebugSwift.swift): disponibilidade de features iOS e factories UIKit para network, app e performance.
- [`DebugSwiftInterfaceSettings.swift`](../DebugSwift/Sources/Features/Interface/DebugSwiftInterfaceSettings.swift) e [`DebugSwiftResourceViewControllers.swift`](../DebugSwift/Sources/Features/Resources/DebugSwiftResourceViewControllers.swift): factories UIKit para interface e resources.
